# Description: MATE project Panel
# Maintainer: Chris Kruger
# Group: mate
# Depends on: intltool ice sm pango gtk glib gio librsvg dbus-glib-1 libmateweather mate-desktop libmatecomponentui mate-menus libwnck

name=mate-panel
base=1.2
version=1.2.1
release=1
source=(http://pub.mate-desktop.org/releases/$base/$name-$version.tar.xz)

build() {
	cd $name-$version
	./autogen.sh --prefix=/usr --sysconfdir=/etc \
	--mandir=/usr/man \
	--localstatedir=/var \
	--libexecdir=/usr/lib/mate-panel \
	--disable-static --disable-scrollkeeper \
	--enable-introspection --enable-matecomponent
	make
	make DESTDIR=$PKG install
	rm -rf $PKG/usr/share/{gtk-doc,locale}
}
