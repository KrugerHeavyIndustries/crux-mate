# Description: A multi-platform toolkit for creating graphical user interfaces. (MATE specific configuration)
# URL: http://www.gtk.org/
# Maintainer: Chris Kruger crux-maint at krugerheavyindustries dot com
# Depends on: atk, hicolor-icon-theme, gdk-pixbuf, pango, xorg-libsm, xorg-libxcursor, xorg-libxinerama, xorg-libxrandr, xorg-libxcomposite, gobject-introspection

name=gtk
version=2.24.17
release=2
source=(http://download.gnome.org/sources/gtk+/${version:0:4}/${name}+-$version.tar.xz \
        gtk.immodules gtk-register.sh)

build () {
    cd gtk+-$version

    ./configure --prefix=/usr --mandir=/usr/man --enable-introspection
    make
    make DESTDIR=$PKG install
    mkdir -p $PKG/usr/etc/gtk-2.0
    install -m 644 $SRC/gtk.immodules $PKG/usr/etc/gtk-2.0/
    rm -r $PKG/usr/share/{locale,gtk-doc,gtk-2.0} $PKG/usr/bin/gtk-demo

    install -m 0755 $SRC/gtk-register.sh $PKG/usr/bin/gtk-register
}
