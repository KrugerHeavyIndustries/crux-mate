# Description: MATE project UI Component library
# Maintainer: Chris Kruger
# Group: mate
# Depends on: mate-common intltool libmatecanvas libmatecomponent libmate mate-conf gtk

name=libmatecomponentui
base=1.2
version=1.2.0
release=2
source=(http://pub.mate-desktop.org/releases/$base/$name-$version.tar.xz)

build() {
	cd $name-$version
	./autogen.sh --prefix=/usr \
	--localstatedir=/var --disable-static
	make
	make DESTDIR=$PKG install
	rm -rf $PKG/usr/share/{gtk-doc,locale}
}
