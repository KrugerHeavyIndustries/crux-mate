# Description: A set of interfaces for accessibility (with MATE specific config)
# URL: http://www.gtk.org/
# Maintainer: Chris Kruger crux-maint at krugerheavyindustries dot com
# Depends on: glib gobject-introspection

name=atk
version=2.6.0
release=3
source=(http://download.gnome.org/sources/$name/${version:0:3}/$name-$version.tar.xz)

build () {
    cd $name-$version
    ./configure --prefix=/usr --enable-introspection
    make
    make DESTDIR=$PKG install
    rm -r $PKG/usr/share/{gtk-doc,locale}
}
