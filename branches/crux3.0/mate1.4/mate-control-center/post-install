#!/bin/sh

PORT=mate-control-center

SCHEMAS=$(pkginfo -l ${PORT} | grep etc/mateconf/schemas/.*\.schemas\$)

for S in ${SCHEMAS}
do
	MATECONF_CONFIG_SOURCE="xml::/usr/etc/mateconf/mateconf.xml.defaults" \
		mateconftool-2 --makefile-install-rule /${S}
done
