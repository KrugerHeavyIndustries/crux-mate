# Description: MATE project Panel
# Maintainer: Chris Kruger
# Group: mate
# Depends on: intltool pango gtk glib librsvg libmateweather mate-desktop mate-menus libmatewnck libcanberra dconf dbus-glib

name=mate-panel
version=1.6.0
release=1
source=(http://pub.mate-desktop.org/releases/${version:0:3}/$name-$version.tar.xz)

build() {
	cd $name-$version
    NOCONFIGURE=1 ./autogen.sh
	./configure --prefix=/usr \
		--mandir=/usr/man \
		--libexecdir=/usr/lib/$name \
		--disable-static \
        --disable-scrollkeeper \
		--enable-introspection
	make
	make DESTDIR=$PKG install
	rm -rf $PKG/usr/share/{gtk-doc,locale}
}
