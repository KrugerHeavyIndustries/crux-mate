# Description: A library for layout and rendering of text
# URL: http://www.gtk.org/
# Maintainer: Fredrik Rinnestam, fredrik at crux dot nu
# Depends on: cairo, glib, xorg-libsm, xorg-libxext, xorg-libxft, gobject-introspection

name=pango
version=1.30.1
release=1
source=(http://download.gnome.org/sources/$name/1.30/$name-$version.tar.xz \
        pango.modules)

build () {
        cd $name-$version
        ./configure --prefix=/usr --mandir=/usr/man --enable-introspection=yes
        make
        make DESTDIR=$PKG install
        install -D -m 644 $SRC/pango.modules $PKG/usr/etc/pango/pango.modules
      	rm -rf $PKG/usr/share/{gtk-doc,locale}
}
