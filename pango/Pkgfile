# Description: A library for layout and rendering of text (MATE configuration)
# URL: http://www.gtk.org/
# Maintainer: Chris Kruger crux-maint at krugerheavyindustries dot com 
# Depends on: harfbuzz, glib, xorg-libsm, xorg-libxext, xorg-libxft, gobject-introspection

name=pango
version=1.32.5
release=3
source=(http://download.gnome.org/sources/$name/${version:0:4}/$name-$version.tar.xz \
	modules.cache)

build () {
	cd $name-$version
	./configure --prefix=/usr --mandir=/usr/man --enable-introspection=yes
	make
	make DESTDIR=$PKG install
	install -D -m 644 $SRC/modules.cache $PKG/usr/lib/$name/1.8.0/modules.cache
	rm -rf $PKG/usr/share/{gtk-doc,locale}
}
